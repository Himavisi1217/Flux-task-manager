[build]
  command = "npm run build"
  publish = "dist"

[dev]
  command = "npm run dev"

[build.environment]
  NODE_VERSION = "21"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
